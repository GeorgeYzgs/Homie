package com.spring.group.models.user;

/**
 * @author George.Giazitzis
 * Enumerated values to separate the users into distinct roles with different authorities within spring security
 */
public enum UserRole {

    USER,
    MODERATOR,
    ADMIN;
}
